# 시스템 구조 및 명령어 (스켈레톤) 🦴

본 프로젝트는 **프론트엔드/백엔드 모노레포(Monorepo)** 구조를 채택하고 있습니다.

---

## 🏗 1. 전체 아키텍처

```text
Indy7_HMI_Clean/                   # Git 저장소 루트
│
├── frontend/                       # 🖥️ 데스크톱 UI (맥/윈도우에서 직접 실행)
│   ├── run_ui_only.py              # 진입점: UI + 로봇 제어만
│   ├── main.py                     # 진입점: 전체 연동 (PLC/MQTT/MES)
│   ├── presentation/ui/            # 화면 계층
│   │   ├── theme.py                # 디자인 토큰 (색상/폰트 중앙 관리)
│   │   ├── main_window.py          # 메인 윈도우 (Page1↔Page2 전환, 폴링 루프)
│   │   ├── robot_hmi/              # Page 2: 티칭/설정 모드
│   │   │   ├── robot_hmi_view.py   # 트리 UI + 프로그램 실행 엔진
│   │   │   ├── editors/            # 노드별 속성 에디터
│   │   │   └── tools/              # 오실로스코프, 팔레타이징 마법사
│   │   └── digital_twin/           # Page 1: 실시간 모니터링 3D 뷰어
│   ├── core/                       # 비즈니스 로직 계층
│   │   ├── domains/robot/          # 로봇 제어 도메인 (IndyDCP, JOG, 이동)
│   │   ├── domains/motion_management/ # 모션 데이터 모델
│   │   └── domains/teaching_management/ # Conty JSON 파서
│   ├── infrastructure/             # 외부 연결 계층
│   │   ├── mqtt/mqtt_manager.py    # MQTT Pub/Sub 매니저
│   │   └── repositories/          # DB Repository
│   └── indy_utils/                 # Neuromeka IndyDCP SDK
│
├── backend/                        # 📦 Docker 마이크로서비스
│   ├── docker-compose.yml          # 전체 서비스 오케스트레이션
│   ├── docker/                     # 각 서비스별 Dockerfile
│   ├── robot_controller/           # 🤖 로봇 상태 폴링 + 명령 수신
│   │   ├── src/main.py
│   │   ├── src/domain/
│   │   ├── src/infrastructure/
│   │   └── tests/
│   ├── digital_twin/               # 🌍 웹소켓 3D 스트리밍 서버
│   │   ├── src/main.py
│   │   └── tests/
│   ├── vision_yolo/                # 👁 카메라 YOLO 추론
│   │   ├── src/main.py
│   │   └── tests/
│   ├── plc_bridge/                 # ⚙️ PLC 센서 폴링
│   │   ├── src/main.py
│   │   └── tests/
│   └── db_worker/                  # 🗄 DB 적재 워커
│       ├── src/main.py
│       └── tests/
│
└── (루트 문서들)
    ├── readme.md                   # 프로젝트 전체 가이드
    ├── history.md                  # 작업 이력
    ├── requirement.md              # 패키지 설치 가이드
    └── 스켈레톤.md                  # 이 파일
```

---

## 💡 데이터 흐름 (MQTT 기반)

```
[PLC Bridge] --plc/sensor/part_arrived--> [MQTT Broker] --robot/command--> [Robot Controller]
                                              |
[Vision YOLO] --vision/target_coord----------->|
                                              |
[Robot Controller] --robot/realtime----------->|--> [DB Worker] --> MySQL DB
                                              |
                                              |--> [Digital Twin] --> 웹소켓 --> 브라우저 3D
```

---

## 💻 2. 필수 명령어

### 🖥️ Frontend (UI) 실행
```bash
# 로봇 제어 + UI만 (테스트용)
cd frontend && python run_ui_only.py

# 전체 시스템 연동 (PLC/MQTT/MES)
cd frontend && python main.py
```

### 📦 Backend (Docker) 실행
```bash
# 백엔드 전체 서비스 빌드 & 실행
cd backend && docker-compose up -d

# 특정 서비스만 실행 (예: DB Worker)
cd backend && docker-compose up db_worker

# 로그 확인
cd backend && docker-compose logs -f robot_controller

# 전체 종료
cd backend && docker-compose down
```

### 🧪 테스트 (TDD)
```bash
# 특정 마이크로서비스 단위 테스트
cd backend/db_worker && pytest tests/
cd backend/plc_bridge && pytest tests/
```
