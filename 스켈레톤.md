# 시스템 구조 및 명령어 (스켈레톤) 🦴

이 문서는 Indy7_HMI_Clean 프로젝트의 전체적인 아키텍처 뼈대(Skeleton)와 터미널에서 자주 사용하는 명령어들을 설명합니다. 새로운 개발자나 유지보수 담당자가 프로젝트의 구조를 빠르게 파악할 수 있도록 돕습니다.

---

## 🏗 1. 시스템 구조 (Architecture Skeleton)

본 프로젝트는 **도메인 주도 설계(DDD)와 클린 아키텍처(Clean Architecture)** 사상을 기반으로 관심사를 철저히 분리했습니다.

```text
Indy7_HMI_Clean/
├── core/                               # [의존성 0%] 핵심 비즈니스 및 도메인 로직 (UI/통신 몰름)
│   ├── domains/                        
│   │   ├── robot/                      # 로봇의 본질적 제어 로직 (이동, 상태 갱신)
│   │   ├── motion_management/          # 모션 데이터 모델 (좌표, 궤적)
│   │   └── teaching_management/        # 프로그램 파싱 모델 (Conty JSON 해석)
│   └── application/use_cases/          # 각 도메인을 엮어 작업을 수행하는 오케스트레이터
│
├── infrastructure/                     # [외부 연결] 통신/DB/파일 시스템 등 외부 기술 구현체
│   ├── db/                             # MES 데이터베이스 연동 (PyMySQL)
│   ├── mqtt/                           # 외부 기기와의 MQTT 통신 구현체
│   └── repositories/                   # 파일 I/O 및 데이터베이스 통신 클래스 (database_repository.py)
│
├── presentation/ui/                    # [화면] 사용자 인터페이스 (CustomTkinter)
│   ├── main_window.py                  # Page 1 (실시간 모니터링 & 3D 뷰어) 및 루프 진입점
│   └── robot_hmi/                      # Page 2 (로봇 프로그램 에디터 & 실행 엔진)
│       ├── robot_hmi_view.py           # 트리 구조 UI 및 프로그램 실행(Execution) 제어
│       └── editors/                    # 각 노드(Pick, Place, Move 등)별 상세 설정 화면
│
└── indy_utils/                         # [SDK] Neuromeka 제공 IndyDCP (소켓 통신 클라이언트)
```

### 💡 데이터 흐름 (Data Flow)
1. **수집 (Polling):** `main_window.py`의 `_poll_loop`에서 초당 10번씩 IndyDCP를 통해 로봇 좌표(q, t_pos, torque)를 읽음.
2. **저장/모니터링:** 수집된 데이터는 HMI 화면(3D 뷰어)에 뿌려지며, 동시에 `database_repository.py`를 통해 외부 DB에 UPSERT 됨.
3. **명령/실행:** `robot_hmi_view.py`에서 파싱된 노드 트리를 바탕으로 `RobotControlUseCase`에 이동/동작 명령 하달. 완료 시 이력 테이블에 Insert.

---

## 💻 2. 필수 명령어 (Commands)

터미널(Terminal)에서 시스템을 구동하거나 코드를 관리할 때 사용하는 명령어 모음입니다.
명령어는 항상 프로젝트 루트 폴더(`Indy7_HMI_Clean`) 안에서 실행해야 합니다.

### 🚀 앱 실행
```bash
# 전체 시스템(DB, PLC 연동 포함) 실행
python main.py

# UI 및 로봇 통신 기능 위주로 가볍게 테스트용 실행 (추천)
python run_ui_only.py
```

### 📦 의존성(패키지) 설치
새로운 패키지가 추가되었거나 환경을 새로 구축할 때:
```bash
# 기본 패키지 설치
pip install -r requirements.txt

# DB 통신 등 추가 패키지 개별 설치
pip install pymysql
```

### 🛠 Git 버전 관리 (코드 업데이트)
작업한 코드를 GitHub에 올리거나 가져올 때 사용합니다.
```bash
# 상태 확인 (변경된 파일 목록)
git status --short

# 모든 변경사항 올리기 준비 (Staging)
git add .

# 커밋 (메시지 작성)
git commit -m "feat: 메시지 내용"

# GitHub(원격 저장소)로 전송
git push

# 다른 사람의 업데이트 가져오기
git pull
```

### 🐞 디버깅 로그 확인
실행 중 발생한 이슈나 로그를 확인할 때 (logs 폴더에 자동 생성됨)
```bash
# 최신 실행 로그 꼬리표 확인 (실시간 출력)
tail -f logs/exec_20260513_184831.log

# 로그에서 특정 단어 찾기 (예: 에러, 정지)
grep -i "에러" logs/exec_*.log
```
