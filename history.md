# 작업 이력 (History) 📝

본 문서는 프로젝트 진행 중 발생한 주요 이슈와 해결 과정, 그리고 업데이트 내역을 기록하는 파일입니다.

## 📅 2026-05-13: 핵심 제어 로직 안정화 및 MES 연동

### 🚀 업데이트 내역 (오늘의 작업)

1. **Pick/Place 인터리빙(Interleaving) 시퀀스 적용**
   - 기존에는 팔레트에 있는 모든 Pick(P1~P6)을 한 번에 실행한 뒤 Place를 실행하는 문제가 있었음.
   - **수정:** 실행 트리 파서(`_execute_node_list`)를 개선하여, Pick(팔레트) 노드를 만나면 다음 노드들 중 파트너(Place)를 미리 탐색(Look-ahead)함.
   - **결과:** `Pick(P1) → 사이 노드(Home) → Place → Pick(P2) → 사이 노드(Home) → Place...` 형태로 실제 공정에 맞게 동작하도록 완벽 동기화 달성.

2. **MES 데이터베이스 연동 (PyMySQL)**
   - 로봇의 실시간 상태와 작업 완료 이력을 외부 MySQL DB(`192.168.3.141`)로 전송하는 `database_repository.py` 작성.
   - **실시간 상태:** 100ms 폴링 루프에서 각 관절의 각도(q)와 제어 토크(torque)를 수집하여 DB에 전송. 데이터가 쌓이지 않도록 `ON DUPLICATE KEY UPDATE` (UPSERT) 적용. 커넥션 부하를 막기 위해 Persistent Connection 구현.
   - **작업 이력:** Pick 또는 Place 동작이 타겟 지점에서 완료된 직후, 동작의 이름과 XYZ 좌표를 DB에 Insert.

3. **팔레타이징 Z축(다단 적재) 보정 알고리즘 패치**
   - `M=1, N=1, L=5`로 배열 설정 시, 행/열 이동이 발생하고 Z축 상승이 안 되는 수학적 버그 발견.
   - **수정:** `compute_pallet_point`에서 M, N이 1일 때 계산 오차를 막고, 층(L) 데이터인 P4가 없을 경우 Z축 기준 0.1m씩 강제 오프셋 되도록 예외 처리 추가.

4. **루프(Loop) 종료 및 강제 정지 안정화**
   - 루프 카운트(예: 5회) 종료 후 프로그램이 정지하지 않거나, Stop 버튼을 눌러도 `wait_for_move_finish` 함수가 30초 동안 블로킹되는 문제 해결.
   - **수정:** 루프 탈출 직후 정지 플래그 재확인. `RobotControlUseCase`에 글로벌 정지 플래그(`_global_stop`)를 도입하여 정지 버튼 클릭 시 대기 중인 모든 모션 명령 즉시 스킵 및 `stop_emergency()` 즉시 호출.

### 💡 주요 인사이트 (문제 해결 팁)
- **로봇 상태 데이터 누락:** HMI에서 보내는 JSON에 `toolId`가 임의로 덮어씌워지며 매핑 정보가 끊기는 현상이 있었음. 원본 데이터(`__raw__`)를 보존하고 fallback을 적용하여 해결.
- **Mac 로컬 방화벽 이슈:** 봇(터미널 백그라운드) 환경에서 DB 서버로 Ping/접속 시 `Operation not permitted` 가 발생하는 샌드박스 정책 발견. 실제 사용자 터미널에서는 정상적으로 통신됨을 확인.
